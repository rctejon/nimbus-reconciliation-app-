services:
  - type: web
    name: nimbus-reconciliation-engine
    env: python
    buildCommand: pip install -r requirements.txt && python3 scripts/generate_test_data.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
